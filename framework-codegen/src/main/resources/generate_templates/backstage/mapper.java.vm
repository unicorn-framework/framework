package ${mapperContext.pkg};

import ${entityContext.classImportPath};
import org.unicorn.framework.mybatis.mapper.CompostionMapper;
/**
 * <p>
 * ${table.comment} Mapper 接口
 * </p>
 *
 * @author ${author}
 * @since ${date}
 */
public interface ${mapperContext.name} extends CompostionMapper<${entity}> {
}